node{

stage('Gradle Build') {
    if (isUnix()) {
        sh 'gradle build'
    } else {
        bat 'gradle.bat clean build'
    }
}

stage('spotbugs scan') {
        sh 'gradle spotbugsMain'

    }
}

